# Projet

## Backend

### Prérequis

Installation à prévoir :

```
npm init -y
npm install express cors dotenv pg swagger-ui-express swagger-jsdoc jsonwebtoken pdfkit
npm install --save-dev jest supertest
npm install nodemon --save-dev
```
